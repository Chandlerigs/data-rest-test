spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/uni_test_open_search
    username: postgres
    password: P@ssw0rd
  jpa:
    database: postgresql
    show-sql: true
    hibernate:
      ddl-auto: update
